# Assignment 4 – Files, Exceptions, and Errors in Python

## Task 1
Reads a text file named `sample.txt` and prints its contents line by line.
Handles errors gracefully if the file does not exist.

## Task 2
Takes user input, writes it to `output.txt`, appends additional data,
and displays the final file contents.

## How to Run
```bash
python task1_read_file.py
python task2_write_append_file.py
